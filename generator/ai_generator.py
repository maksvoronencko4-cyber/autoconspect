"""
AI Генератор текстов — Wikipedia + Gemini
"""

import google.generativeai as genai
from datetime import datetime
from .wikipedia_source import WikipediaSource


class AIGenerator:
    
    def __init__(self, api_key: str = None):
        self.model = None
        self.is_ready = False
        self.model_name = None
        self.wiki = WikipediaSource(language='ru')
        
        if api_key:
            self._init_model(api_key)
    
    def _init_model(self, api_key: str):
        """Инициализация модели Gemini"""
        try:
            genai.configure(api_key=api_key)
            
            models_to_try = [
                'gemini-1.5-flash',
                'gemini-1.5-pro',
                'gemini-pro',
                'gemini-1.0-pro',
            ]
            
            for model_name in models_to_try:
                try:
                    model = genai.GenerativeModel(model_name)
                    response = model.generate_content("Тест")
                    if response.text:
                        self.model = model
                        self.model_name = model_name
                        self.is_ready = True
                        print(f"✅ Модель: {model_name}")
                        return
                except:
                    continue
            
            self.is_ready = False
            
        except Exception as e:
            print(f"Ошибка API: {e}")
            self.is_ready = False
    
    def generate(self, mode: str, topic: str, volume: str, style: str, 
                 author_info: dict = None) -> str:
        """Генерация текста с данными из Википедии"""
        
        if not self.is_ready:
            return "❌ API не настроен. Проверьте ключ Gemini."
        
        # 1. Получаем данные из Википедии
        wiki_data = self.wiki.get_data_for_topic(topic)
        
        # 2. Строим промпт
        prompt = self._build_prompt(mode, topic, volume, style, wiki_data)
        
        try:
            # 3. Генерируем через Gemini
            response = self.model.generate_content(prompt)
            result = response.text
            
            # 4. Добавляем источники
            if wiki_data.get('success') and wiki_data.get('source'):
                result += f"\n\n{'='*50}\n📚 ИСТОЧНИКИ:\n{wiki_data['source']}\n{'='*50}"
            
            # 5. Титульная страница
            if author_info and author_info.get('include_title') and mode in ['referat', 'doklad', 'essay']:
                title_page = self._generate_title_page(topic, author_info, mode)
                result = title_page + "\n\n" + result
            
            return result
            
        except Exception as e:
            return f"❌ Ошибка генерации: {str(e)}"
    
    def _build_prompt(self, mode: str, topic: str, volume: str, style: str, wiki_data: dict) -> str:
        """Построение промпта с данными из Википедии"""
        
        volume_text = {
            "short": "1-2 страницы",
            "medium": "3-5 страниц",
            "long": "6-10 страниц",
            "very_long": "10-15 страниц"
        }.get(volume, "3-5 страниц")
        
        style_text = {
            "scientific": "научный академический стиль",
            "simple": "простой понятный язык",
            "school": "язык для школьника",
            "university": "язык для студента университета"
        }.get(style, "научный стиль")
        
        # Контекст из Википедии
        if wiki_data.get('success'):
            wiki_context = f"""
ДАННЫЕ ИЗ ВИКИПЕДИИ ПО ТЕМЕ "{topic}":
══════════════════════════════════════════════════════════

{wiki_data['content']}

══════════════════════════════════════════════════════════

ИНСТРУКЦИЯ:
- Используй ТОЛЬКО информацию из данных выше
- НЕ придумывай факты, которых нет в источнике
- Перефразируй своими словами, не копируй дословно
- Структурируй информацию логично
"""
        else:
            wiki_context = f"""
⚠️ В Википедии не найдено статей по теме "{topic}".
Напиши работу, используя общие знания, но отметь что информация требует проверки.
"""
        
        # Промпты для разных режимов
        prompts = {
            "referat": f"""{wiki_context}

ЗАДАНИЕ: Напиши реферат на тему "{topic}"

ТРЕБОВАНИЯ:
- Объём: {volume_text}
- Стиль: {style_text}
- Язык: русский

СТРУКТУРА РЕФЕРАТА:

# РЕФЕРАТ
## Тема: {topic}

## ВВЕДЕНИЕ
(актуальность, цель, задачи — 1-2 абзаца)

## ГЛАВА 1. [Название по теме]
### 1.1 [Подраздел]
(содержательный текст с фактами из источника)

### 1.2 [Подраздел]
(продолжение)

## ГЛАВА 2. [Название]
### 2.1 [Подраздел]
(текст)

## ЗАКЛЮЧЕНИЕ
(выводы по работе)

## СПИСОК ЛИТЕРАТУРЫ
(источники будут добавлены автоматически)

Пиши содержательно, используя факты из Википедии!""",

            "conspect": f"""{wiki_context}

ЗАДАНИЕ: Напиши конспект на тему "{topic}"

ТРЕБОВАНИЯ:
- Объём: {volume_text}
- Стиль: {style_text}

СТРУКТУРА:

# 📝 КОНСПЕКТ: {topic}

## 🔑 Ключевые понятия
**Термин** — определение
(3-5 основных терминов)

## 📚 Основные положения

### 1. [Раздел]
• Тезис
• Тезис
• Тезис

### 2. [Раздел]
• Тезис
• Тезис

### 3. [Раздел]
• Тезис

## ✅ Выводы
1. Главный вывод
2. Второй вывод
3. Третий вывод

## 💡 Запомнить
• Самое важное""",

            "doklad": f"""{wiki_context}

ЗАДАНИЕ: Напиши текст доклада на тему "{topic}"

ТРЕБОВАНИЯ:
- Объём: {volume_text}
- Стиль: {style_text}
- Формат: текст для устного выступления

СТРУКТУРА:

# 📊 ДОКЛАД
## Тема: {topic}

## ВСТУПЛЕНИЕ
Здравствуйте! Сегодня я расскажу о теме "{topic}".
(почему тема важна, 2-3 предложения)

## ОСНОВНАЯ ЧАСТЬ

### [Первый пункт]
(рассказ с фактами, 2-3 абзаца)

### [Второй пункт]
(продолжение)

### [Третий пункт]
(если нужно)

### Интересные факты
• Факт 1
• Факт 2

## ЗАКЛЮЧЕНИЕ
(итоги, 2-3 предложения)

Спасибо за внимание! Готов ответить на вопросы.""",

            "question": f"""{wiki_context}

ЗАДАНИЕ: Дай полный ответ на вопрос: "{topic}"

ТРЕБОВАНИЯ:
- Объём: {volume_text}
- Стиль: {style_text}

СТРУКТУРА:

# ❓ Вопрос: {topic}

## ✅ Краткий ответ
(прямой ответ, 2-3 предложения)

## 📖 Подробный ответ
(развёрнутое объяснение на основе данных из Википедии)

## 📝 Примеры
1. Пример
2. Пример

## 🎯 Итог
(заключение)""",

            "retell": f"""{wiki_context}

ЗАДАНИЕ: Сделай пересказ материала на тему "{topic}"

ТРЕБОВАНИЯ:
- Объём: {volume_text}
- Стиль: {style_text}

СТРУКТУРА:

# 📖 ПЕРЕСКАЗ: {topic}

## О чём материал
(краткое содержание, 2-3 предложения)

## Подробный пересказ
(изложение информации своими словами)

## Главные мысли
• Мысль 1
• Мысль 2
• Мысль 3""",

            "essay": f"""{wiki_context}

ЗАДАНИЕ: Напиши эссе на тему "{topic}"

ТРЕБОВАНИЯ:
- Объём: {volume_text}
- Стиль: {style_text}
- Формат: эссе с личными размышлениями

СТРУКТУРА:

# ✍️ ЭССЕ: {topic}

*"[Подходящая цитата известного человека]"*

## Вступление
(введение в тему, личное отношение)

## Размышления

### [Первый аспект]
(рассуждение с аргументами на основе фактов)

### [Второй аспект]
(другая точка зрения)

### [Третий аспект]
(возможно, контраргументы)

## Заключение
(личные выводы, финальная мысль)"""
        }
        
        return prompts.get(mode, prompts["referat"])
    
    def _generate_title_page(self, topic: str, author_info: dict, mode: str) -> str:
        """Титульная страница"""
        
        work_type = {
            "referat": "РЕФЕРАТ",
            "doklad": "ДОКЛАД", 
            "essay": "ЭССЕ"
        }.get(mode, "РЕФЕРАТ")
        
        edu_type = author_info.get('edu_type', 'Студент')
        grade = author_info.get('grade', '1')
        name = author_info.get('name', '')
        institution = author_info.get('institution', '')
        group = author_info.get('group', '')
        teacher = author_info.get('teacher', '')
        
        grade_text = f"{grade} класса" if edu_type == "Ученик" else f"{grade} курса"
        year = datetime.now().year
        
        lines = [
            "=" * 60,
            "",
            institution.upper() if institution else "[УЧЕБНОЕ ЗАВЕДЕНИЕ]",
            "",
            "-" * 60,
            "",
            work_type,
            "",
            "на тему:",
            f'«{topic}»',
            "",
            "-" * 60,
            "",
            "Выполнил(а):",
            f"{edu_type} {grade_text}",
        ]
        
        if name:
            lines.append(name)
        if group:
            lines.append(f"Группа: {group}")
        if teacher:
            lines.extend(["", f"Преподаватель: {teacher}"])
        
        lines.extend(["", "-" * 60, "", f"{year} год", "", "=" * 60])
        
        return "\n".join(lines)
